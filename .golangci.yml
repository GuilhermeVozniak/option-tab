version: "2"
run:
  timeout: 5m
linters:
  default: standard
  enable:
    - errcheck
    - ineffassign
    - misspell
    - staticcheck
    - unused
formatters:
  enable:
    - gofmt
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true
