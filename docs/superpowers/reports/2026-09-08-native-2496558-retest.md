# Native retest of 2496558 — 2026-09-08

The new build fixes the previously observed clock clipping and permits authenticated local AppleScript queries. Folder-child, preview and focus-action checks still failed, with their exact native causes unresolved. Blacklist creation and translation defects were also confirmed in source. This is not an all-clear for the retained roadmap.

Follow-up: [September 9 fixes and corrected folder result](2026-09-09-native-retest-fixes.md) supersede the unresolved findings below. A controlled read-only CG/AX check confirmed that the unchanged installed build opens a separate folder child; parent-scoped screenshots alone were insufficient to assess it.

The user-installed `/Applications/option-tab-local-2496558.app` remained running as PID `58044`. Its executable matched source `2496558891544b89c95b1e9202cda65ee39258f5` and SHA-256 `c5842157c12dc2008c77ba3dde2b648382ff2f65ccbecec3a681334fb65a79e8` before testing and after restoration. Build metadata records version 0.4.8, arm64, macOS 14.0 minimum and `vcs.modified=false`. The bundle passed strict ad hoc signature verification; it is not Developer ID signed or notarized. No replacement native build or app restart occurred during this retest.

## Automated verification

Fresh execution against that exact commit passed:

- `TURBO_FORCE=true GOFLAGS=-count=1 task test`: 480 desktop, 7 shared and 4 website tests; all 25 Go packages with race detection and coverage. No cached test results were used.
- Desktop Playwright: 110 Chromium tests, including ten widget geometry cases.
- Dedicated WebKit layout suite: ten tests covering all four edges, constrained hosts and the four-slot viewport cap.

All three commands exited 0. Browser runs used two headless workers and no retries. Existing macOS linker/deprecation and React testing warnings remain in the logs. Browser tests use a fake Wails backend; they rebuilt frontend assets only and do not establish native behavior. Lint and release packaging were not rerun in this pass.

## Native observations

| Flow | Observed result |
|---|---|
| Settings export | Native Save and Cancel worked. The saved JSON was mode 0600 and matched the baseline exactly. A collision returned an error and left the existing export unchanged. |
| Profile duplication and export | The duplicate retained its edited name in the actual exported profile. |
| H17 import and repair | Review showed one item, one widget and three sanitization notices. Cancel preserved the current state; confirmation created an unassigned profile with access removed. Selecting the owned folder again and saving persisted its private reference without `cleanupFailed`. |
| Timezone | An incomplete invalid value produced feedback; the valid `Europe/Rome` value persisted. |
| Widget package lifecycle | Installation granted no implicit access. Explicit grant and enable worked. Removal disabled the remaining instance, cleared grants and removed the private package. |
| Clock layout and stacks | Both clock texts were visibly inside the live LG Dock in bottom and left orientations. Creating a stack, switching its live clock to UTC and dissolving the stack worked. This resolves the prior two-clock visual failure for those tested layouts. |
| Media connection | Spotify reached Connected immediately. The slow Connecting state and its cancellation/lifetime behavior were not observed natively. |
| Switcher modes | AppleScript opened and rendered both window and app switchers. Mouse selection activated the owned TextEdit `One.txt` document. Setting the app appearance to Titles left the window appearance at Thumbnails. Physical shortcut delivery was not established. |
| Automation queries | Application and window queries returned valid JSON. An active-window query succeeded for the owned TextEdit document. An earlier active query while Preferences was foreground returned `unavailable`; that refusal remains unexplained. The previous local-sender identity rejection did not recur. |
| Minimize action | The API returned success for owned window `1566`; independent native AX verification at 21:30:52 UTC confirmed `AXMinimized=true`. |
| Settings and diagnostics | All seven Settings tabs were visited. Filtering dropdowns received smoke coverage only. Diagnostics review and its native Save chooser opened; Escape cancelled. No diagnostic file export was completed. |

## Failures and remaining issues

- **Folder child:** Activating the repaired owned folder did not produce an observable child. At 21:28:45 UTC, AX and CG metadata showed only the visible parent Dock, frame `(3755, 2231, 810, 64)`. No visible child remained. The startup rejection stage is unknown; this does not prove that the earlier readiness race was the only cause.
- **Focus action:** Focusing owned window `1566` returned `unavailable: native application rejected the action` (-2700). It partially changed state: AX subsequently reported unminimized and main, CG reported on-screen, and TextEdit was frontmost. The window reported `AXFocused=false` at 21:31:41 UTC, but that window attribute is not authoritative evidence of focus; the application's `AXFocusedWindow` identity was not recorded. The returned error establishes the failure. The subsequent September 9 investigation reproduced a 100 ms `AXRaise` timeout while AppKit restored an owned minimized window.
- **App previews:** `show app previews` for TextEdit returned `staleIdentity: window identity changed`. Read-only source review found that the all-window identity guard runs before preview filters, so an excluded auxiliary inventory window can block eligible previews. The native inventory is consistent with that path, but the particular rejected window was not identified.
- **Blacklist creation:** Clicking Add twice produced no usable row. The source submits an empty matcher immediately; canonical normalization drops empty matchers. This confirms a native integration gap that the passing browser tests did not expose.
- **Localization:** PT-BR and Spanish showed the English background-capture label and explanatory paragraph. Both strings are missing from their translation catalogs.
- **Minor feedback issue:** General settings import restored the configuration while a prior export-collision notice remained visible.

## Acceptance limits and restoration

Physical keyboard/trackpad delivery, haptic sensation, media panels and playback, lyrics, Space changes, lock/restart and permission transitions, preview dragging, focused-app profile rules, badge recovery and the full layout/display matrix were not established. H15 pinch/swipe/letter input, automatic native Dock relocation and Spotify seek remain deliberately unavailable in production. Signed universal distribution, supported-OS testing and Homebrew lifecycle remain separate release gates. Removed sibling-product overlaps remain outside Option Tab's scope.

Root restored preferences through General import of the actual settings export, then removed the unused owned folder reference. Independent comparison at 21:39:16 UTC verified the canonical settings were byte-for-byte identical to the original baseline: 6,467 bytes, SHA-256 `0a75abba2be980f06a0cf939164dc2d820767d2fe235216d0c73f83fc953a60b`, with no JSON differences. Private reference and package stores each contained zero entries. PID `58044` and its executable hash were unchanged. The owned TextEdit document was closed, its resulting chooser cancelled, and TextEdit quit; root's process check confirmed PID `78253` exited while Option Tab remained running. The user's untracked `refactor-notes/` was untouched.

Evidence is local in the ignored `.superpowers/sdd/native-ui-2496558-2026-09-08/` directory: `automated/` holds full logs, exact commands and exits; JSON records cover build identity, exports, package lifecycle, automation, AX/CG postconditions and final restoration. Visual observations came from native screenshots in the conversation; no screenshot files were persisted. The earlier automated summary predates these native outcomes; this report records the completed retest.
