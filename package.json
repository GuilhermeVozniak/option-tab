{
  "name": "option-tab",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.1.38",
  "engines": {
    "bun": ">=1.1",
    "node": ">=20"
  },
  "workspaces": ["apps/*", "apps/desktop/frontend", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.6.0"
  }
}
